import { Bootstrapper } from '../../core/bootstrap/Bootstrapper.ts';

export class RoomsBootstrapper extends Bootstrapper {
    override async registerBindings(): Promise<void> {
        return super.registerBindings();
    }
}